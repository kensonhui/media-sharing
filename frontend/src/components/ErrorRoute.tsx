import { Container, Text } from "@chakra-ui/react";

const ErrorRoute = () => (
  <Container>
    <Text>
      The page you were looking for wasn't found. Double check the url.
    </Text>
  </Container>
);

export default ErrorRoute;
